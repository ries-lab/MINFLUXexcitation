classdef seqMF<handle
    properties
        L
        laserpower
        scouting
        localization
    end
    methods
        function pos=run(obj)
            %iterate over L, laserpower, max iterations
        end

    end
end
